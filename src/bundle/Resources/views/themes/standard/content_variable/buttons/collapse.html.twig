<a
    data-toggle="collapse"
    class="btn content-variables-collapsable-collections"
    role="button"
    aria-expanded="{{ isCollapsed ? 'false' : 'true' }}"
    href="#{{ containerId }}"
    data-cookie-variable="{{ cookieVariable|default(false) }}"
    data-cookie-separator="{{ cookieSeparator|default(false) }}"
    data-item-id="{{ itemId|default('') }}"
>
    <svg class="ibexa-icon collapse-show"{% if not isCollapsed %} hidden{% endif %}>
        <use xlink:href="{{ asset('bundles/ibexaadminui/img/ibexa-icons.svg') }}#caret-down"/>
    </svg>
    <svg class="ibexa-icon collapse-hide"{% if isCollapsed %} hidden{% endif %}>
        <use xlink:href="{{ asset('bundles/ibexaadminui/img/ibexa-icons.svg') }}#caret-up"/>
    </svg>
</a>