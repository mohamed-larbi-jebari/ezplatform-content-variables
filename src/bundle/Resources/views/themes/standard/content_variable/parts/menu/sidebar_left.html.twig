{% extends '@ezdesign/parts/menu/sidebar_base.html.twig' %}

{% block item -%}
    {%- set default_classes = 'btn btn-dark btn-block' ~ (matcher.isCurrent(item) ? ' ' ~ options.currentClass) -%}
    {{ parent() }}
{%- endblock %}
